---
"zarrita": minor
---

Add `dimension_names` support to `create()` and expose `dimensionNames` getter on `Array`. For v2 arrays, `_ARRAY_DIMENSIONS` from attributes is automatically mapped to `dimensionNames`.
